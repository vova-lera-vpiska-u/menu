Frontend app uses FSD.
This app should be a PWA
Typescript `as` and `!` are FORBIDDEN
Short/one-letter variable names are FORBIDDEN
Use subtle web UI animations and transitions
Use `types > consts > effects > gates > stores > events > logic from start to end > utility functions` architecture in effector models
Use `clock > source > filter > fn > target` order in sample
Never use `getState` or `watch`
Use sample nesting to narrow types
Any useEffect must be moved to a named hook
Split files if takes more than 500 lines
